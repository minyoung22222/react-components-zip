const colors = {
  whiteColor: '#FFFFFF',
  blackColor: '#000000',
};

const mq = {
  desktop: '@media only screen and (min-width: 2560px)',
  mobile: '@media only screen and (min-width: 360px)',
};

const theme = {
  colors,
  mq,
};

export default theme;
